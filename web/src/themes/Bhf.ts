import { createMuiTheme } from "@material-ui/core/styles";

const Bhf = createMuiTheme({
    custom: {
        icon: "#000",
        loadingBar: "#000",
    },
    palette: {
        primary: {
            main: "#E2001A",
        },
        background: {
            default: "#DCE4E8",
            paper: "#DCE4E8",
        },
    },
});

export default Bhf;
